{
  "name": "ppt-alhee-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ALHEE — Saisie PPT (Plan Pluriannuel de Travaux) — projet restructuré (Phase 1)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "extract": "node scripts/extract-assets.mjs",
    "make-icons": "node scripts/make-icons.mjs",
    "generate-reference": "node scripts/generate-reference.mjs",
    "diff-pptx": "node scripts/diff-pptx.mjs",
    "test:no-regression": "node scripts/test-no-regression.mjs"
  },
  "devDependencies": {
    "jszip": "^3.10.1",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "@azure/msal-browser": "^5.15.0"
  }
}
